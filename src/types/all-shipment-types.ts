export type shipmentProps = {
    id: number,
    name: string,
    pickup: string,
    dropoff: string,
    date: string,
    time: string,
    status: string,
    border: string,
    textColor: string
    to?: string
}
