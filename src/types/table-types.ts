export type TableProps = {
    vendor: string,
    riders: string,
    dropoff: string,
    pickup: string,
    date: string,
    time: string,
    Status: string,
    border?: string,
    textColor?: string,
    to: string,
}